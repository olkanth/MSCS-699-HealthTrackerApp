# Copy this file to .env and fill in real values. .env is gitignored (*.env)
# so it never gets committed.

DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/dbhealthtracker
SECRET_KEY=change-me-to-a-random-64-char-hex-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
